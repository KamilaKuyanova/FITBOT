# Инструкция по запуску проекта FitApplication

## 🚀 Быстрый запуск

### Вариант 1: Запуск через отдельные терминалы (рекомендуется)

#### Шаг 1: Запустить Backend сервер

Откройте первый терминал и выполните:

```powershell
cd "FIT APPLICATION\server"
npm start
```

Сервер запустится на порту **3001** (по умолчанию).

#### Шаг 2: Запустить Mobile приложение

Откройте второй терминал и выполните:

```powershell
cd "FIT APPLICATION\mobile"
npm start
```

Это запустит Expo DevTools. Затем:
- Нажмите **`a`** для запуска на Android эмуляторе
- Нажмите **`i`** для запуска на iOS симуляторе (только Mac)
- Или отсканируйте QR код в приложении Expo Go на физическом устройстве

---

## 📱 Запуск на физическом устройстве

Если вы хотите запустить на реальном телефоне:

1. **Установите Expo Go** на телефон:
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Найдите IP-адрес вашего компьютера:**
   ```powershell
   ipconfig
   ```
   Найдите "IPv4 Address" (например, `192.168.0.23`)

3. **Настройте API URL для mobile приложения:**
   Создайте файл `.env` в папке `mobile/`:
   ```
   EXPO_PUBLIC_API_BASE_URL=http://192.168.0.23:3001
   ```
   Замените `192.168.0.23` на ваш IP-адрес.

4. **Убедитесь, что телефон и компьютер на одной Wi-Fi сети**

5. **Запустите с tunnel режимом:**
   ```powershell
   cd "FIT APPLICATION\mobile"
   npx expo start --tunnel
   ```

6. **Отсканируйте QR код** в приложении Expo Go

---

## ⚙️ Если зависимости не установлены

Если при запуске возникают ошибки, установите зависимости:

```powershell
# В папке server
cd "FIT APPLICATION\server"
npm install

# В папке mobile
cd "FIT APPLICATION\mobile"
npm install
```

---

## 🐛 Возможные проблемы

### Backend не запускается
- Убедитесь, что порт 3001 свободен
- Проверьте, что установлены зависимости: `npm install`

### Mobile не подключается к backend
- Для физического устройства: обязательно настройте `.env` файл с вашим IP
- Убедитесь, что backend сервер запущен и доступен
- Проверьте, что телефон и компьютер на одной Wi-Fi сети

### Expo не запускается
- Попробуйте очистить кеш: `npx expo start -c`
- Убедитесь, что установлены зависимости: `npm install`

---

## 📝 Полезные команды

```powershell
# Очистить кеш Expo
cd "FIT APPLICATION\mobile"
npx expo start -c

# Запустить только на Android
cd "FIT APPLICATION\mobile"
npm run android

# Запустить только на iOS (Mac only)
cd "FIT APPLICATION\mobile"
npm run ios

# Запустить в браузере (для тестирования)
cd "FIT APPLICATION\mobile"
npm run web
```

---

## ✅ Проверка, что всё работает

1. Backend сервер: Откройте http://localhost:3001/health в браузере - должно вернуть `{"ok":true}`
2. Mobile приложение: Должен открыться Expo DevTools с QR кодом
3. Приложение на устройстве: Должно загрузиться и показать главный экран

