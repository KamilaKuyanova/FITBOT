# 📱 Запуск через Expo Go - Инструкция

## Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

Просто выполните в PowerShell:

```powershell
cd "FIT APPLICATION"
.\start-expo-go.ps1
```

Этот скрипт:
- ✅ Создаст `.env` файл с правильным IP адресом
- ✅ Запустит backend сервер в отдельном окне
- ✅ Запустит Expo с tunnel режимом (для физических устройств)

---

### Вариант 2: Ручной запуск

#### Шаг 1: Настройте .env файл

Создайте файл `.env` в папке `mobile/`:

```bash
EXPO_PUBLIC_API_URL=http://192.168.0.15:3001
```

**⚠️ Важно:** Замените `192.168.0.15` на IP адрес вашего компьютера, если он другой.

Как узнать свой IP:
```powershell
ipconfig
# Найдите "IPv4 Address" в разделе вашего Wi-Fi адаптера
```

#### Шаг 2: Запустите Backend сервер

Откройте первый терминал:

```powershell
cd "FIT APPLICATION\server"
npm start
```

Сервер запустится на порту **3001**.

#### Шаг 3: Запустите Expo

Откройте второй терминал:

```powershell
cd "FIT APPLICATION\mobile"
npx expo start --tunnel
```

Флаг `--tunnel` необходим для подключения физических устройств через Expo Go.

---

## 📲 Подключение через Expo Go

### 1. Установите Expo Go на телефон

- **iOS**: [App Store - Expo Go](https://apps.apple.com/app/expo-go/id982107779)
- **Android**: [Google Play - Expo Go](https://play.google.com/store/apps/details?id=host.exp.exponent)

### 2. Убедитесь, что телефон и компьютер на одной Wi-Fi сети

Оба устройства должны быть подключены к одной сети Wi-Fi.

### 3. Отсканируйте QR код

После запуска `npx expo start --tunnel`:
- В терминале появится QR код
- Или откройте браузер и перейдите по адресу, который показан в терминале
- Откройте приложение **Expo Go** на телефоне
- Нажмите **"Scan QR Code"**
- Отсканируйте QR код

### 4. Приложение загрузится на телефон

После сканирования приложение начнет загружаться и через несколько секунд откроется на вашем телефоне.

---

## ⚙️ Настройки

### Изменение IP адреса

Если IP адрес вашего компьютера изменился, обновите `.env` файл:

1. Откройте `mobile/.env`
2. Измените IP адрес на новый:
   ```
   EXPO_PUBLIC_API_URL=http://НОВЫЙ_IP:3001
   ```
3. Перезапустите Expo (Ctrl+C и затем `npx expo start --tunnel`)

### Использование tunnel режима

Tunnel режим (`--tunnel`) позволяет подключаться к приложению даже если телефон и компьютер на разных сетях. Это медленнее, но работает везде.

**Альтернативы:**
- `npx expo start` - LAN режим (быстрее, но требует одну сеть)
- `npx expo start --localhost` - только эмулятор/симулятор

---

## 🐛 Решение проблем

### Не подключается к backend

**Проблема:** Приложение загружается, но показывает ошибки подключения к API.

**Решения:**
1. Проверьте, что backend сервер запущен (`npm start` в папке `server`)
2. Проверьте `.env` файл - IP адрес должен быть правильным
3. Проверьте, что телефон и компьютер на одной Wi-Fi сети
4. Попробуйте открыть `http://ВАШ_IP:3001/health` в браузере на телефоне - должно показать `{"ok":true}`

### QR код не сканируется

**Решения:**
1. Убедитесь, что Expo Go обновлен до последней версии
2. Увеличьте яркость экрана
3. Попробуйте ввести URL вручную в Expo Go (кнопка "Enter URL manually")

### Приложение не загружается

**Решения:**
1. Проверьте интернет соединение на телефоне
2. Попробуйте остановить и перезапустить Expo: `npx expo start --tunnel --clear`
3. Убедитесь, что порты не заблокированы файрволом

---

## ✅ Проверка работоспособности

После успешного запуска:

1. **Backend**: Откройте `http://localhost:3001/health` в браузере → должно вернуть `{"ok":true}`
2. **Expo**: В терминале должна появиться ссылка типа `exp://192.168.x.x:8081`
3. **Приложение**: На телефоне должно открыться приложение и показать главный экран

---

## 📝 Полезные команды

```powershell
# Очистить кеш и перезапустить
cd "FIT APPLICATION\mobile"
npx expo start --tunnel --clear

# Запуск без tunnel (быстрее, но требует одну сеть)
npx expo start

# Просмотр логов
npx expo start --tunnel | Select-String "QR"

# Остановить сервер
# В терминале нажмите Ctrl+C
```

---

## 🎯 Что дальше?

После успешного запуска:
- ✅ Приложение работает на телефоне через Expo Go
- ✅ Все изменения в коде будут автоматически перезагружаться (Hot Reload)
- ✅ Можно тестировать все функции приложения

**Примечание:** Expo Go используется только для разработки. Для продакшена нужно создать standalone build через EAS Build или Expo Build.

